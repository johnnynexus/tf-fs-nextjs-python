# Committed defaults for `next dev`. Non-secret only.
# `.env.local` overrides anything here, and real process env vars override both.
BACKEND_INTERNAL_URL=http://localhost:8000
NEXT_PUBLIC_RELEASE=dev
